[package]
name = "tsync-migrate"
description = "Run rewrite through verify, and optionally handoff, as one command."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true

[dependencies]
thiserror.workspace = true
tsync-handoff = { path = "../handoff" }
tsync-import = { path = "../import" }
tsync-plan = { path = "../plan" }
tsync-qbt = { path = "../qbt" }
tsync-rewrite = { path = "../rewrite" }
tsync-transfer = { path = "../transfer" }
tsync-verify = { path = "../verify" }

[dev-dependencies]
tempfile.workspace = true
tsync-fixtures = { path = "../fixtures" }
tsync-rewrite = { path = "../rewrite" }

[lints]
workspace = true
